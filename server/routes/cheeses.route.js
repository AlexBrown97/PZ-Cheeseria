// import express from "express";
// const router = express.Router();

// router.get("/api", (req, res) => {
//   res.json({ cheeses });
// });

// export default router;

// Tried splitting out into routes but was getting two errors: cannot import module, require not defined.
// If had more time I would have seperated the routes out
